# calc config
server.modules = ( "mod_alias", "mod_cgi" )

server.port = 8000
server.document-root        = "/var/empty"
server.dir-listing          = "disable"

alias.url= ( "" => "/data/calc.sh" )
cgi.assign = ( "" => "/data/calc.sh" )
cgi.execute-x-only = "enable"

